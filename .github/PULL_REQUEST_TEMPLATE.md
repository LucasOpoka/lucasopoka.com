## What changed

## Why

## Checklist
- [ ] `npm run lint && npm run format:check && npm run typecheck && npm run test` pass locally
- [ ] Added/updated tests for this change, or explained why not needed
- [ ] Verified in the browser locally (`docker compose -f docker-compose.local.yml up --build`) if
      this touches UI or routing
