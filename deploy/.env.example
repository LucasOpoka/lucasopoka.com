# Copied to .env in each environment's directory on the VPS
# (/opt/lucasopoka/production/.env, /opt/lucasopoka/staging/.env).
# deploy.sh rewrites IMAGE_TAG in place on every deploy; CONTAINER_NAME is set once.

IMAGE_TAG=latest
CONTAINER_NAME=lucasopoka-web
